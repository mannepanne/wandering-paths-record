# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment files
.env
.env.local
.env.production.local
.env.development.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
coverage/

# Database backups (contain real data, never commit)
db_backup/

# Wrangler local state (miniflare D1 emulator, KV cache, etc.)
.wrangler/state/
.wrangler/tmp/

# Claude Code runtime state (scheduled task locks, etc.)
.claude/scheduled_tasks.lock

# Per-clone review system override (enables dogfooding without changing committed default)
.claude/project-config.local.json
